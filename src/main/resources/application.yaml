server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ireesh/config-repository.git
          search-paths: test1
          clone-on-start: true
  application:
    name: config_server

logging:
  level:
    org.springframework.cloud: DEBUG
